import { KeyboardShortcut as KeyboardShortcutComponent } from "@/components/ui/keyboard-shortcut";

const KeyboardShortcutDemo = () => {
  // You can pass props here if needed, like controlling the animation.
  return <KeyboardShortcutComponent animated={true} />;
};

export { KeyboardShortcutDemo };